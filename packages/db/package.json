{
  "name": "@greenfield/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "echo \"@greenfield/db: no TS source to check\" && exit 0",
    "build": "echo \"@greenfield/db: no build step (SQL is the artifact)\" && exit 0",
    "lint": "echo \"@greenfield/db: no lint configured\" && exit 0",
    "generate": "supabase gen types typescript --linked > src/database.types.ts && echo \"Wrote src/database.types.ts\"",
    "push": "tsx scripts/db-push.ts",
    "push:dry": "tsx scripts/db-push.ts --dry",
    "push:mark": "tsx scripts/db-push.ts --mark"
  },
  "dependencies": {
    "pg": "^8.22.0",
    "supabase": "^1.207.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "tsx": "^4.23.0",
    "typescript": "6.0.3"
  }
}